---
phase: 03-editable-table-full-generation-pipeline-persistence-deployment
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/app/main.py
  - backend/tests/test_generation.py
  - frontend/src/api/client.ts
  - frontend/src/components/SegmentTable.tsx
autonomous: true
requirements: [TBL-03]
must_haves:
  truths:
    - "User can check a per-row checkbox (or a header select-all) to select multiple segments"
    - "When 1+ rows are selected, a toolbar appears above the table offering reassign-narrator to a chosen character"
    - "Confirming the bulk action reassigns every selected row's narrator in one request"
  artifacts:
    - backend/app/main.py
    - frontend/src/components/SegmentTable.tsx
  key_links:
    - "bulk-reassign endpoint validates every segment id belongs to the same project as the target character before applying (merge_character ownership discipline)"
    - "Row selection uses getRowId: (s) => s.id so selection survives row-order changes"
---

<objective>
Add multi-row selection and a bulk narrator reassignment toolbar to the segment table (TBL-03). Builds directly on the SegmentTable from plan 03-01.

Purpose: TBL-03 — select multiple rows and reassign their narrator/voice in one action.
Output: A checkbox column + header select-all, a bulk-action toolbar, and a validated bulk-reassign endpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-CONTEXT.md
@.planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-RESEARCH.md
@.planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing test + bulk-reassign endpoint with cross-project ownership validation</name>
  <files>backend/tests/test_generation.py, backend/app/main.py</files>
  <read_first>
    - backend/tests/test_generation.py (seeding + TestClient convention from plan 03-01)
    - backend/tests/test_wizard_endpoints.py (merge_character ownership-validation test precedent, if present)
    - backend/app/main.py (merge_character lines 398-458 — the project_id ownership check to mirror; patch_segment from plan 03-01)
    - .planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-RESEARCH.md (Security Domain: bulk-reassign validation)
  </read_first>
  <behavior>
    - test_bulk_reassign_updates_all_rows: POST /segments/bulk-reassign with a list of segment ids and a target character_id sets every listed segment's character_id to the target; response reports the count updated.
    - test_bulk_reassign_bumps_generation_version: each reassigned segment's generation_version increments (a narrator change alters the resolved speaker, so cached audio is now stale).
    - test_bulk_reassign_rejects_cross_project: a request whose target character belongs to a different project than the segments returns 4xx and changes nothing.
  </behavior>
  <action>
    Add the three tests to backend/tests/test_generation.py. Implement POST /segments/bulk-reassign in main.py: a BulkReassignRequest Pydantic model (segment_ids: list[str], character_id: str); in one Session, load the target character and verify each segment's project_id equals the target character's project_id (mirror merge_character's source.project_id != target.project_id 404 discipline — reject the whole request if any mismatches, changing nothing); set each segment's character_id to the target and bump generation_version; commit and return the updated count. Do NOT auto-synthesize here (bulk regen is the batch action in plan 03-03) — just mark rows stale by bumping generation_version. Run `cd backend && uv run ruff check .` and fix warnings.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_generation.py -x && uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - The three bulk-reassign tests pass.
    - A cross-project request returns a 4xx and mutates no rows.
    - `cd backend && uv run ruff check .` reports no warnings.
  </acceptance_criteria>
  <done>Bulk-reassign endpoint reassigns validated segments in one request and rejects cross-project tampering.</done>
</task>

<task type="auto">
  <name>Task 2: Checkbox column + bulk-action toolbar in SegmentTable</name>
  <files>frontend/src/components/SegmentTable.tsx, frontend/src/api/client.ts, frontend/src/components/ui/checkbox.tsx</files>
  <read_first>
    - frontend/src/components/SegmentTable.tsx (the table from plan 03-01 being extended)
    - frontend/src/api/client.ts (wrapper convention; Segment/SegmentPatch from plan 03-01)
    - .planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-RESEARCH.md (Pattern 2 checkbox row selection, getRowId)
    - .planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-UI-SPEC.md (checkbox component is NEW; bulk toolbar 48px bar; accent reserve for checked state + confirm button)
  </read_first>
  <action>
    Install the checkbox primitive: `cd frontend && npx shadcn add checkbox`. Add a leading columnHelper.display({id:"select"}) column to SegmentTable with a header select-all Checkbox (table.getIsAllRowsSelected / getToggleAllRowsSelectedHandler) and a per-row Checkbox (row.getIsSelected / getToggleSelectedHandler), wiring rowSelection state + onRowSelectionChange and getRowId:(s)=>s.id per Pattern 2. Add bulkReassignSegments(segmentIds, characterId) to client.ts following the existing wrapper convention. Render a 48px-high bulk-action toolbar above the table that appears only when table.getSelectedRowModel().rows.length > 0, showing an "N selected" label, a Select of the project characters, and a confirm button (accent) that calls bulkReassignSegments with the selected ids and refreshes the table, then clears selection. Non-destructive, no confirmation dialog (UI-SPEC copywriting contract). Run `cd frontend && npm run build` and fix type errors.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` succeeds.
    - frontend/src/components/ui/checkbox.tsx exists (shadcn add).
    - SegmentTable renders a header select-all checkbox and per-row checkboxes, and a toolbar that only appears when rows are selected.
    - grep confirms `getRowId` and `bulkReassignSegments` appear in the changed frontend files.
  </acceptance_criteria>
  <done>User can select rows via checkboxes and reassign their narrator in one toolbar action.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03-02)

- `backend/app/main.py`: `BulkReassignRequest`, `POST /segments/bulk-reassign`
- `frontend/src/api/client.ts`: `bulkReassignSegments()`
- `frontend/src/components/ui/checkbox.tsx` (shadcn primitive, new)
- `frontend/src/components/SegmentTable.tsx`: select column + bulk toolbar
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tailnet client → FastAPI | Bulk-reassign request carries an arbitrary list of segment ids + a target character id |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | POST /segments/bulk-reassign | high | mitigate | Validate every segment id's project_id matches the target character's project_id before applying; reject the whole request on any mismatch (mirrors merge_character line 413). Prevents reassigning another project's segments |
| T-03-05 | Tampering (Input Validation) | BulkReassignRequest body | low | mitigate | Pydantic model with typed list[str] + str fields |
| T-03-SC | Tampering | package installs | low | accept | Only shadcn `checkbox` from the official registry (UI primitive, not a runtime dependency); no pip/npm package added. No install checkpoint required |
</threat_model>

<verification>
- Backend: `cd backend && uv run pytest tests/test_generation.py && uv run ruff check .`
- Frontend: `cd frontend && npm run build`
</verification>

<success_criteria>
- Multi-row selection via checkboxes + header select-all (TBL-03).
- Bulk toolbar reassigns all selected rows' narrator in one validated request (TBL-03).
</success_criteria>

<output>
Create `.planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-02-SUMMARY.md` when done.
</output>
