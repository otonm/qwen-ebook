---
phase: 03-editable-table-full-generation-pipeline-persistence-deployment
plan: 04
type: execute
wave: 4
depends_on: ["03-01", "03-03"]
files_modified:
  - backend/app/main.py
  - backend/tests/test_generation.py
  - frontend/src/api/client.ts
  - frontend/src/components/ProjectListScreen.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [PERS-01, PERS-02]
must_haves:
  truths:
    - "The root screen lists all saved projects (filename, date, status badge) with an Open action and a New Project button"
    - "Opening a project from the list restores its segment table, cast, cached audio, and progress exactly where it was left"
    - "Projects are saved automatically as the user works (no explicit Save)"
    - "An empty project list shows the 'No projects yet' empty state"
  artifacts:
    - backend/app/main.py
    - frontend/src/components/ProjectListScreen.tsx
    - frontend/src/App.tsx
  key_links:
    - "GET /projects reads the existing Project table (no schema change) and returns filename/date/status per row"
    - "ProjectListScreen selecting a project sets the App's active projectId, reusing PROJECT_ID_STORAGE_KEY"
---

<objective>
Add the project list / reopen flow (PERS-02) and make it the app's landing screen, plus confirm auto-save (PERS-01) holds by construction. After this plan a user lands on a list of saved projects, opens any one to continue exactly where they left off, or starts a new one.

Purpose: PERS-01 (auto-save as user works), PERS-02 (reopen a saved project).
Output: GET /projects list endpoint, ProjectListScreen, App routing landing on the list.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-CONTEXT.md
@.planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-RESEARCH.md
@.planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing test + GET /projects list endpoint</name>
  <files>backend/tests/test_generation.py, backend/app/main.py</files>
  <read_first>
    - backend/tests/test_generation.py (seeding + TestClient convention)
    - backend/app/main.py (get_project lines 189-202 read shape to adapt; _serialize_project)
    - backend/app/models.py (Project fields incl. created_at added in plan 03-01)
    - .planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-RESEARCH.md (Pattern 6 list_projects; A3 created_at)
  </read_first>
  <behavior>
    - test_list_projects_returns_saved_projects: seed two projects; GET /projects returns both with id, filename, status, and a date field (created_at), ordered deterministically (newest first or by created_at).
    - test_list_projects_empty: with no projects, GET /projects returns an empty list (200), not an error.
  </behavior>
  <action>
    Add the two tests to backend/tests/test_generation.py. Implement GET /projects (list_projects) in main.py per RESEARCH.md Pattern 6: read all Project rows in one Session, return a list of {id, filename, status, created_at} ordered by created_at (the column added in plan 03-01). Do not include full segment/character payloads — this is a thin list. Run `cd backend && uv run ruff check .` and fix warnings.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_generation.py -k projects && uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - Both list tests pass; empty DB returns `[]` with 200.
    - `cd backend && uv run ruff check .` reports no warnings.
  </acceptance_criteria>
  <done>GET /projects returns saved projects with filename/date/status over the existing table, no schema change.</done>
</task>

<task type="auto">
  <name>Task 2: ProjectListScreen + App routing landing on the list</name>
  <files>frontend/src/api/client.ts, frontend/src/components/ProjectListScreen.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/components/UploadScreen.tsx (screen-level component shape + navigate-on-action)
    - frontend/src/App.tsx (PROJECT_ID_STORAGE_KEY + the analysis-stream gating from plan 03-01)
    - frontend/src/api/client.ts (wrapper convention; parseJsonOrThrow)
    - .planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-UI-SPEC.md (Screens: Project List is root route; card rows; status badge; New Project CTA; empty-state + load-failure copy; skeleton loading)
  </read_first>
  <action>
    (1) client.ts: add listProjects() and a ProjectSummary type ({id, filename, status, created_at}) following the existing wrapper convention.
    (2) ProjectListScreen.tsx (new): fetch listProjects on mount; render each project as a card row (filename, formatted date, status badge) with an "Open" action; a persistent accent "New Project" button at the top that enters the existing Phase 2 upload flow; a Skeleton loading state; the "No projects yet" empty state + "Upload a book to create your first project." copy; and the "Couldn't load your projects." load-failure copy (all verbatim from the UI-SPEC copywriting contract).
    (3) App.tsx: make the project list the root/landing screen — when no active projectId, show ProjectListScreen (not the upload screen directly); selecting a project sets projectId (reuse PROJECT_ID_STORAGE_KEY / setProjectId) which then flows into the existing analyzing/wizard/ProjectScreen states; "New Project" routes to the upload flow; add a persistent "← Projects" affordance on ProjectScreen that returns to the list without losing state (autosave-by-construction). Confirm PERS-01 holds: every edit/PATCH already commits immediately — add a one-line comment noting no separate save mechanism is needed rather than building one.
    Run `cd frontend && npm run build` and fix type errors.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` succeeds.
    - ProjectListScreen renders project cards with filename/date/status, an Open action, a New Project button, and the empty-state copy when the list is empty.
    - App.tsx renders ProjectListScreen as the landing screen when there is no active projectId.
    - grep confirms `listProjects` is called in ProjectListScreen and `ProjectListScreen` is rendered in App.tsx.
  </acceptance_criteria>
  <done>A user lands on a project list, opens any saved project to resume exactly where they left off, or starts a new one.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03-04)

- `backend/app/main.py`: `GET /projects` (`list_projects`)
- `frontend/src/api/client.ts`: `listProjects()`, `ProjectSummary` type
- `frontend/src/components/ProjectListScreen.tsx`
- `frontend/src/App.tsx`: landing-on-list routing + "← Projects" navigation
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tailnet client → FastAPI | GET /projects list request (read-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Information Disclosure | GET /projects returns all projects | low | accept | Single-user tailnet tool with no multi-tenant model; every project belongs to the one user. Tailnet membership is the access boundary (REQUIREMENTS Out of Scope: multi-user auth) |
| T-03-11 | Tampering (Input Validation) | project_id path param on reopen | low | mitigate | Existing get_project already 404s on unknown/ill-formed ids; list endpoint is read-only with no user-supplied filter |
| T-03-SC | Tampering | package installs | low | accept | No new pip/npm packages. No install checkpoint required |
</threat_model>

<verification>
- Backend: `cd backend && uv run pytest tests/test_generation.py && uv run ruff check .`
- Frontend: `cd frontend && npm run build`
</verification>

<success_criteria>
- Project list screen lists saved projects and reopens any to continue where left off (PERS-02).
- Auto-save holds by construction — every edit commits immediately, no explicit Save (PERS-01).
</success_criteria>

<output>
Create `.planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-04-SUMMARY.md` when done.
</output>
