---
phase: 03-editable-table-full-generation-pipeline-persistence-deployment
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - deploy/qwen-ebook.pod
  - deploy/qwen-ebook-tts.container
  - deploy/qwen-ebook-backend.container
  - deploy/README.md
autonomous: false
user_setup:
  - service: tailscale
    why: "Expose the running app to the tailnet only, with no public port and no added auth layer (DEPL-02)"
    dashboard_config:
      - task: "Run `sudo tailscale serve --bg 8000` once on the production VM to proxy the loopback-bound backend onto the tailnet (persists across reboots)"
        location: "Production RX 9070 XT VM (Tailscale hostname `tts`), over Tailscale SSH"
requirements: [DEPL-02]
must_haves:
  truths:
    - "The two-container pod (GPU-scoped TTS + CPU backend) runs as systemd-managed Podman Quadlet units, not a manual dev script"
    - "The backend is published only to loopback and reached from the tailnet solely via `tailscale serve` — no public all-interfaces port"
    - "The GPU TTS container keeps both /dev/kfd and /dev/dri plus User=0/Group=0, matching run-local.sh exactly"
    - "The app is reachable from a second tailnet device and unreachable off-tailnet"
  artifacts:
    - deploy/qwen-ebook.pod
    - deploy/qwen-ebook-tts.container
    - deploy/qwen-ebook-backend.container
  key_links:
    - "AddDevice for both /dev/kfd and /dev/dri plus User=0/Group=0 in the TTS .container unit (the flag set most often dropped in translation)"
    - "PublishPort binds loopback only in the .pod unit — tailscale serve is the only tailnet path"
---

<objective>
Translate the imperative run-local.sh pod bring-up into declarative Podman Quadlet units and put the whole running app behind `tailscale serve` on the production RX 9070 XT VM — the final v1 deployment slice (DEPL-02). After this plan the complete app runs as a systemd-managed service reachable only over the tailnet.

Purpose: DEPL-02 — Tailscale-only exposure, no public exposure, no added auth layer, deployed as Podman Quadlet units.
Output: A `.pod` plus two `.container` Quadlet units, a documented systemd + tailscale bring-up, and a verified tailnet-only reachability check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-CONTEXT.md
@.planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-RESEARCH.md
@.planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Quadlet pod + two container units from run-local.sh</name>
  <files>deploy/qwen-ebook.pod, deploy/qwen-ebook-tts.container, deploy/qwen-ebook-backend.container</files>
  <read_first>
    - deploy/run-local.sh (whole file — the imperative podman pod create + two podman run blocks these units translate; especially the device/port/env flags around lines 54-84)
    - deploy/README.md (Production VM bring-up + D-09 GPU re-verification checklist — the rootful invocation shape and post-deploy device verification)
    - backend/GPU-ENABLEMENT.md (why rootful and root-in-container is required — do NOT modify this file per its header note)
    - .planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-RESEARCH.md (Pattern 7 exact unit bodies; Pitfall 4 dropped-device-flag; Environment Availability: reconfirm Podman version on the VM)
    - .planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-PATTERNS.md (device-flag/port/env mapping table for the deploy files)
  </read_first>
  <action>
    Create the three Quadlet unit files under deploy/ per RESEARCH.md Pattern 7, cross-checked line-by-line against run-local.sh's podman invocations so NO flag is silently dropped (Pitfall 4):
    (1) deploy/qwen-ebook.pod: a [Pod] section naming the pod qwen-ebook, publishing the backend port bound to loopback only (not all-interfaces — tailscale serve is the tailnet path), with the TTS port 8001 kept pod-internal (no publish entry for it), and an [Install] WantedBy=multi-user.target.
    (2) deploy/qwen-ebook-tts.container: a [Container] section with the TTS image (localhost/qwen-ebook-tts:dev), Pod= pointing at the pod unit, User=0 and Group=0, an AddDevice entry for /dev/kfd AND a separate AddDevice entry for /dev/dri, and a Volume for the Hugging Face cache matching run-local.sh's HF cache mount; After= and Requires= the pod service.
    (3) deploy/qwen-ebook-backend.container: a [Container] section with the backend image (localhost/qwen-ebook-backend:dev), Pod= pointing at the pod unit, Environment entries for TTS_BACKEND=http and TTS_SERVICE_URL=http://localhost:8001, and After= the pod plus tts services. No AddDevice here (backend is CPU-only).
    These are rootful units (root's systemd, under the system Quadlet search path) matching the D-09-verified GPU access path. Do NOT modify run-local.sh — it remains the manual dev bring-up; the Quadlet units are the persistent-service form. Note in each file header that the production VM's exact Podman version should be reconfirmed over Tailscale SSH since Quadlet key syntax shifted across Podman 4.4 to 5.x.
  </action>
  <verify>
    <automated>test -f deploy/qwen-ebook.pod && test -f deploy/qwen-ebook-tts.container && test -f deploy/qwen-ebook-backend.container && grep -q 'kfd' deploy/qwen-ebook-tts.container && grep -q 'dri' deploy/qwen-ebook-tts.container && grep -q 'User=0' deploy/qwen-ebook-tts.container && echo UNITS-OK</automated>
  </verify>
  <acceptance_criteria>
    - All three unit files exist under deploy/.
    - The TTS container unit contains AddDevice entries for both /dev/kfd and /dev/dri plus User=0 and Group=0 (Pitfall 4 — none dropped vs run-local.sh).
    - The pod unit publishes the backend port on loopback only, and does NOT publish the TTS 8001 port.
    - The backend container unit sets TTS_BACKEND=http and TTS_SERVICE_URL=http://localhost:8001 and has no AddDevice.
  </acceptance_criteria>
  <done>The two-container GPU-scoped pod is expressed as systemd-managed Quadlet units with no device/port/env flag dropped relative to run-local.sh.</done>
</task>

<task type="auto">
  <name>Task 2: Document the systemd + tailscale-serve bring-up and reachability verification</name>
  <files>deploy/README.md</files>
  <read_first>
    - deploy/README.md (existing Production VM bring-up section to extend; do not rewrite the D-09 checklist, append the Quadlet + tailscale flow)
    - deploy/run-local.sh (the manual flow the README currently documents, for contrast)
    - .planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-RESEARCH.md (Pattern 7 bring-up commands; Open Question 2 — verify tailscale serve reaches the loopback-published pod port; Security Domain unit-file permissions)
  </read_first>
  <action>
    Append a "Quadlet (systemd-managed) deployment" section to deploy/README.md documenting: copying the three unit files into the system Quadlet search path with root-owned 0644 permissions (Security Domain), `sudo systemctl daemon-reload`, and starting the backend unit (which pulls in the pod + TTS units via Requires=); then the one-time `sudo tailscale serve --bg 8000` to expose the loopback-bound backend on the tailnet (persists independently of the Quadlet units); and the post-deploy verification steps — confirm GPU devices are present in the TTS container (podman exec ... ls the two device nodes), confirm the backend answers on the host loopback (curl the healthz endpoint), then confirm reachability from a second tailnet device AND non-reachability off-tailnet. Explicitly flag Open Question 2 (does tailscale serve on the host reach a pod port published to loopback) as something to confirm live on the VM during this step, not assume. Keep run-local.sh documented as the dev/manual path.
  </action>
  <verify>
    <automated>grep -qi 'quadlet' deploy/README.md && grep -q 'tailscale serve' deploy/README.md && echo README-OK</automated>
  </verify>
  <acceptance_criteria>
    - deploy/README.md has a Quadlet deployment section covering daemon-reload, starting the backend unit, and `tailscale serve` exposure.
    - The section lists the GPU-device, loopback-healthz, second-tailnet-device-reachable, and off-tailnet-unreachable verification steps.
    - Unit-file permission guidance (root:root 0644) is documented.
  </acceptance_criteria>
  <done>An operator can bring the app up as a systemd service and expose it tailnet-only from the README alone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify tailnet-only deployment on the production VM</name>
  <action>Deploy the Quadlet units + tailscale serve on the production RX 9070 XT VM and confirm tailnet-only reachability with a real end-to-end generate, per the steps below.</action>
  <what-built>Podman Quadlet units for the two-container pod and a documented `tailscale serve` exposure, deployed on the production RX 9070 XT VM (Tailscale hostname `tts`).</what-built>
  <how-to-verify>
    1. On the VM over Tailscale SSH: copy the three unit files into the system Quadlet path, `sudo systemctl daemon-reload`, `sudo systemctl start qwen-ebook-backend.service`.
    2. Confirm the pod is up and GPU devices are only on the TTS container: `sudo systemctl status qwen-ebook-tts.service` and `sudo podman exec qwen-ebook-tts ls /dev/kfd /dev/dri`.
    3. Confirm the backend answers on host loopback: `curl 127.0.0.1:8000/healthz` returns 200.
    4. `sudo tailscale serve --bg 8000`, then from a SECOND tailnet device open the app's tailnet URL and confirm the project list loads and a small end-to-end generate works.
    5. Confirm the app is NOT reachable from a device off the tailnet (no public exposure).
  </how-to-verify>
  <resume-signal>Type "approved" once the app is reachable from a second tailnet device, GPU devices are confirmed on the TTS container only, and off-tailnet access fails; or describe what broke.</resume-signal>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03-05)

- `deploy/qwen-ebook.pod` (Quadlet pod unit)
- `deploy/qwen-ebook-tts.container` (Quadlet GPU-scoped TTS container unit)
- `deploy/qwen-ebook-backend.container` (Quadlet CPU backend container unit)
- `deploy/README.md`: Quadlet + tailscale-serve deployment section
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tailnet ↔ host | tailscale serve is the only path from any device to the backend |
| host → pod | Backend port published to loopback only; TTS port stays pod-internal |
| unit files → root filesystem | Quadlet units live under root-owned systemd config |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Elevation of Privilege / Spoofing | app exposed with no auth layer | high | accept | Explicitly accepted per project constraints — tailnet membership is the sole trust boundary (REQUIREMENTS Out of Scope: multi-user auth). Mitigated defensively by binding the backend to loopback and exposing only via tailscale serve, never a public all-interfaces port |
| T-03-13 | Information Disclosure | accidental public exposure via all-interfaces bind | high | mitigate | Pod unit publishes the backend port on loopback only; verification step confirms off-tailnet access fails before sign-off (Open Question 2) |
| T-03-14 | Tampering | GPU device flag dropped in Quadlet translation | medium | mitigate | Both /dev/kfd and /dev/dri AddDevice entries + User=0/Group=0 verified in the unit and via podman exec device check post-deploy (Pitfall 4) |
| T-03-15 | Tampering | world-writable unit files | low | mitigate | Root-owned 0644 unit files documented; no secret material in the units (env vars are non-sensitive TTS_BACKEND/TTS_SERVICE_URL) |
| T-03-SC | Tampering | package installs | low | accept | No new pip/npm packages; Podman Quadlet + tailscale are host tooling already present per D-01/D-02. No install checkpoint required |
</threat_model>

<verification>
- Static: the Task 1 and Task 2 automated greps confirm the units and README content exist and carry the load-bearing flags.
- Live (human-verify checkpoint): app reachable from a second tailnet device, GPU devices confirmed on the TTS container only, off-tailnet access fails, and a small end-to-end generate works on the real GPU pod (closes the CONTEXT D-02 real-hardware gap for the deployed shape).
</verification>

<success_criteria>
- App runs as systemd-managed Podman Quadlet units with GPU scoped to the TTS container (DEPL-02).
- App is reachable only over the tailnet via tailscale serve, with no public exposure and no added auth layer (DEPL-02).
</success_criteria>

<output>
Create `.planning/phases/03-editable-table-full-generation-pipeline-persistence-deployme/03-05-SUMMARY.md` when done.
</output>
