---
phase: 07-unified-generate-stop-play-button-trimmed-segment-table
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/src/components/ConfigPanel.tsx
autonomous: true
requirements: [GEN-10, GEN-11, GEN-12]
must_haves:
  truths:
    - "Each ConfigPanel character preview row renders exactly one <GenerateStopPlayButton size=\"sm\"> (no separate icon Play + text Stop pair): amber 'Generate Preview' with no preview, red 'Stop Generation'/'Stopping…' while previewing, green 'Play' once preview_audio_path exists (GEN-10)."
    - "The batch 'Generate All' control is one <GenerateStopPlayButton size=\"default\" className=\"w-full\">; its status derives in the exact order stopping (isCancelling) > generating (isSelfRunning) > ready (hasOutput) > idle — and once the joined output exists that same button becomes green 'Play' toggling a hidden <audio src={outputUrl(project.id)}> for the joined file (GEN-11, D-04)."
    - "GEN-11 adjacency (Pitfall 2, load-bearing): during a re-run of a project that already has output_path set, the batch button shows red 'Stop Generation', NOT green 'Play' — because isSelfRunning is checked before hasOutput (output_path is never cleared at batch-start, only overwritten on join success)."
    - "GEN-11 empty: with no output_path and no run in flight the batch button is amber idle 'Generate Preview'; the hidden joined-output <audio> only renders when hasOutput is true (absent media is impossible by construction)."
    - "GEN-11 concurrency: while generationLocked is held by another site the idle batch button is disabled with title 'Another generation is already running.'; after a stop the button returns to a usable state with no stuck 'Stopping…' (single-flight lock)."
    - "GEN-11 ordering: { statement: The batch control is a singleton with no per-item ordering semantics; no output ordering guarantee applies to a single button, verification: backstop }."
    - "GEN-10 adjacency: a character that is both generating and already has preview_audio_path shows red generating (precedence generating > ready), never green Play."
    - "GEN-10 empty: a character with no preview_audio_path renders amber idle 'Generate Preview'."
    - "GEN-10 encoding: the aria-label interpolates character.name verbatim (any Unicode) via subjectLabel 'preview for {name}' with no truncation."
    - "GEN-10 ordering: character list order is unchanged — collapsing two buttons into one does not reorder the character rows."
    - "GEN-12: editing a character's fields clears preview_audio_path server-side (bumping voice_version) and the button reverts to amber idle via the hook's reactive prop read — no separate status badge survives."
    - "The separate blue Download button (Phase 6, CFG-08) is unchanged and sits below the unified batch button in the same gap-3 Generation section — Play and Download are two distinct buttons once output exists (D-04)."
    - "E3 empty/loading/error/populated: joined-output audio only exists when hasOutput (empty impossible); while joining the button is red 'Stop Generation' with Loader2 and the existing Progress/batchError blocks stay as-is; happy path is green Play/Pause driving the hidden <audio> (UI-SPEC UI Considerations E3)."
  artifacts:
    - "frontend/src/components/ConfigPanel.tsx (modified — CharacterPreviewRow and the batch block both swap to GenerateStopPlayButton; new hidden joined-output <audio> + outputAudioRef/isOutputPlaying state added)"
  key_links:
    - "The batch button's status is derived from existing variables isCancelling, isSelfRunning, hasOutput in that precedence order (reused verbatim, not recomputed)."
    - "The joined-output <audio> uses outputUrl(project.id) from Plan 01 — same /projects/{id}/download route Phase 6 serves per-format."
    - "generationLocked continues to arrive as a prop and gate idle-button disabled state; the batch site keeps its SSE-driven isSelfRunning via useGenerationStream (hook called with poll:false or inline derivation)."
  prohibitions:
    - "No auto-play or auto-download on generation completion — the joined-output Play is user-triggered only (regresses GEN-03's user-triggered precedent otherwise)."
    - "The batch status derivation must check isSelfRunning BEFORE hasOutput — reversing the order would show green Play (and scrub the stale previous joined file) during an active re-run (Pitfall 2 silent-correctness bug)."
    - "The blue Download button must not be merged into or replaced by the unified button — Play and Download stay two separate buttons (D-04)."
---

<objective>
Collapse `ConfigPanel.tsx`'s two remaining hand-rolled implementations — the `CharacterPreviewRow` character-preview control (GEN-10) and the batch Generate All/Stop block (GEN-11) — into the shared `<GenerateStopPlayButton>`. Add the joined-output green Play state (D-04): once `output_path` exists the batch button becomes green Play and toggles in-browser playback of the joined file via a new hidden `<audio>` using `outputUrl`. The blue Download button (Phase 6) stays untouched.

Purpose: Two of the four sites unified; the batch button gains the joined-output preview per GEN-11/D-04.
Output: Modified `ConfigPanel.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-UI-SPEC.md
@.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-RESEARCH.md
@.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-PATTERNS.md
@.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Collapse CharacterPreviewRow to the shared component (GEN-10)</name>
  <files>frontend/src/components/ConfigPanel.tsx</files>
  <read_first>
    - frontend/src/components/ConfigPanel.tsx — CharacterPreviewRow (lines ~55-204): its poll/settle effects, isTriggeringPreview/hasPreview split, the icon Play/Pause + conditional Generate/Stop button JSX (lines ~146-186), the hidden <audio> (lines ~187-195), and the error paragraph
    - frontend/src/components/GenerateStopPlayButton.tsx — prop contract (Plan 01)
    - frontend/src/hooks/useGenerateStopPlay.ts — hook signature (Plan 01)
    - frontend/src/api/client.ts — triggerCharacterPreview, cancelCharacterPreview, previewUrl, errorMessage
  </read_first>
  <action>Replace CharacterPreviewRow's two-button JSX (icon Play/Pause + conditional 'Generate preview'/'Stop') with one `<GenerateStopPlayButton size="sm" ... />` fed by `useGenerateStopPlay({ hasAudio: Boolean(character.preview_audio_path), isExternallyGenerating: <existing "is this character previewing" boolean>, poll: true, onGenerate: () => triggerCharacterPreview(character.id), onStop: () => cancelCharacterPreview(character.id), onRefresh: onCastRefresh })`. Set `subjectLabel={\`preview for ${character.name}\`}` (reuses existing aria wording), `disabled={generationLocked && status === "idle"}`, `disabledReason="Another generation is already running."`. Keep the existing hidden `<audio src={previewUrl(...)}>` element with its onPlay/onPause/onEnded → isPlaying wiring, and keep the per-row error paragraph fed by the hook's `error`. Remove the now-redundant local isTriggeringPreview/isStopping poll state that the hook now owns.</action>
  <verify>
    <automated>cd frontend && grep -q 'GenerateStopPlayButton' src/components/ConfigPanel.tsx && grep -q 'useGenerateStopPlay' src/components/ConfigPanel.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - CharacterPreviewRow renders exactly one GenerateStopPlayButton (no icon Play + separate Stop pair remains)
    - hasAudio maps from character.preview_audio_path
    - subjectLabel interpolates character.name (Unicode-safe)
    - The hidden <audio>/isPlaying playback and per-row error paragraph are preserved
    - `npm run typecheck` and `npm run lint` pass
  </acceptance_criteria>
  <done>Each character preview row shows one amber/red/green button driven by the shared hook+component.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Unify the batch Generate All control + joined-output Play (GEN-11, D-04)</name>
  <files>frontend/src/components/ConfigPanel.tsx</files>
  <read_first>
    - frontend/src/components/ConfigPanel.tsx — the batch block (lines ~458-497), the hasOutput/isSelfRunning/isCancelling derivations (lines ~243-266), the Progress/joinBlocked/batchError surfaces, and the blue Download button (lines ~520-535)
    - .planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-RESEARCH.md — Pitfall 2 (the output_path-never-clears-at-batch-start proof and the load-bearing precedence order)
    - .planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-UI-SPEC.md — Component Contracts §3 (batch integration + status precedence)
    - frontend/src/api/client.ts — outputUrl (Plan 01), runBatchGeneration, cancelBatchGeneration
    - frontend/src/hooks/useGenerationStream.ts — the SSE source feeding isSelfRunning
  </read_first>
  <action>Replace the batch Generate All/Stop button(s) with one `<GenerateStopPlayButton size="default" className="w-full" ... />`. Derive `status` from the EXISTING variables in this exact precedence order (do not reorder): `isCancelling` → "stopping"; else `isSelfRunning` → "generating"; else `hasOutput` → "ready"; else "idle". Reuse `isSelfRunning`, `isCancelling`, `hasOutput` verbatim — the batch site is SSE-driven (useGenerationStream), so either call useGenerateStopPlay with `poll: false` or keep this ~4-line inline derivation. Wire `onGenerate` → runBatchGeneration, `onStop` → cancelBatchGeneration, `onTogglePlay` → toggle a NEW hidden `<audio ref={outputAudioRef} src={outputUrl(project.id)}>` rendered only when `hasOutput`, with new `isOutputPlaying` state driven by its onPlay/onPause/onEnded. Set `subjectLabel="the joined output"`, `disabled={generationLocked && status === "idle"}`, `disabledReason="Another generation is already running."`, `isPlaying={isOutputPlaying}`. Keep the Progress, joinBlocked, and batchError blocks below exactly as-is. Leave the blue Download button (D-04/CFG-08) completely unchanged — it stays a distinct `<Button>` below the unified button in the same gap-3 Generation section. Do NOT auto-play the joined output on completion.</action>
  <verify>
    <automated>cd frontend && grep -q 'outputUrl' src/components/ConfigPanel.tsx && grep -q 'isSelfRunning' src/components/ConfigPanel.tsx && grep -q 'downloadUrl' src/components/ConfigPanel.tsx && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The batch control is one GenerateStopPlayButton with className="w-full"
    - Status precedence source order is isCancelling → isSelfRunning → hasOutput → idle (verifiable by source read; isSelfRunning appears before hasOutput)
    - A hidden <audio src={outputUrl(project.id)}> renders gated on hasOutput; toggled by onTogglePlay; not auto-played
    - The blue Download button (downloadUrl) is still present and unchanged
    - `npm run typecheck`, `npm run lint`, and `npm run build` pass
  </acceptance_criteria>
  <done>The batch button follows the yellow/red/green pattern, becomes green Play for the joined output once it exists (checked AFTER isSelfRunning), and Download remains a separate blue button.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend HTTP | Character-preview and batch buttons dispatch to existing trigger/cancel/download endpoints; joined-output <audio> reads the existing /projects/{id}/download route |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering | batch status derivation | medium | mitigate | Enforce isSelfRunning-before-hasOutput precedence so an active re-run cannot expose a green Play that scrubs the stale previous joined file (Pitfall 2); verified by the human-verify regenerate-with-existing-output check in Plan 05 |
| T-07-06 | Information Disclosure | joined-output <audio> route | low | accept | Reuses the Phase-6-vetted /projects/{id}/download route; no new data path; Tailscale access boundary |
</threat_model>

<verification>
- `cd frontend && npm run typecheck && npm run lint && npm run build` all pass
- Batch status precedence order (isCancelling → isSelfRunning → hasOutput → idle) confirmed by source read
- Hidden joined-output <audio> gated on hasOutput; Download button unchanged
</verification>

<success_criteria>
GEN-10: character preview control is one yellow/red/green button. GEN-11: the Generate All control follows the same pattern and additionally shows green Play for the joined output once ready (never during an active re-run, per Pitfall 2). D-04: Play and Download are two separate buttons. GEN-12: no separate status badge.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- No new exported symbols — `ConfigPanel.tsx` refactored in place. Adds local state `isOutputPlaying` + `outputAudioRef` and a hidden joined-output `<audio>`; both `CharacterPreviewRow` and the batch block now consume `useGenerateStopPlay` + `GenerateStopPlayButton` from Plan 01.
</artifacts_produced>

<output>
Create `.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-03-SUMMARY.md` when done
</output>
