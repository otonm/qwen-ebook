---
phase: 04-immediate-cancellation
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/components/SegmentTable.tsx
  - frontend/src/components/ConfigPanel.tsx
  - frontend/src/components/ProjectScreen.tsx
autonomous: false
requirements: [GEN-06, GEN-07, GEN-08]

must_haves:
  truths:
    - "generateSegment() awaits the 202 accept then polls for completion (no longer awaits a segment body) — matching 04-03's async contract"
    - "A generating segment row shows a bare-bones Stop control that calls the segment cancel endpoint (GEN-06)"
    - "A generating character preview shows a bare-bones Stop control that calls the preview cancel endpoint (GEN-07)"
    - "The batch Stop control interrupts the in-flight segment, and its caveat copy no longer claims the current segment may still finish (GEN-08, D-06)"
    - "Every Stop control shows a distinct transient 'stopping…' state — idle vs generating vs stopping are all visible, not an instant optimistic flip to idle (D-03/D-05)"
  artifacts:
    - frontend/src/api/client.ts
    - frontend/src/components/SegmentTable.tsx
    - frontend/src/components/ConfigPanel.tsx
    - frontend/src/components/ProjectScreen.tsx
  key_links:
    - "generateSegment's contract flip (202 + poll) must match 04-03's backend, or the row never shows completed audio"
    - "the 'stopping…' state stays until the backend confirms the lock released (poll GET /generation-status / refetch), reflecting that a new generation genuinely cannot start yet (Pitfall 2 honesty)"
---

<objective>
Add the minimal, functional frontend for GEN-06/07/08: adapt the segment generate call site to 04-03's async 202+poll contract, give the segment row and the character-preview row bare-bones Stop buttons wired to the new cancel endpoints, add a distinct "stopping…" transient state to all three Stop controls (segment, character preview, batch), and fix the now-false batch Stop caveat copy. Per CONTEXT.md D-04 these buttons are functional-only — NO yellow/red/green unification (that is Phase 7). Finish with a human-verify checkpoint proving the D-01 true-kill end-to-end on real hardware.

Purpose: Expose the real backend interrupt to the user with honest state (D-03/D-05: "stopping…" is required, not optional polish), without pulling Phase 7's unified button forward.
Output: `cancelSegmentGeneration`/`cancelCharacterPreview` client fns; Stop + "stopping…" on segment rows, character-preview rows, and the batch control; updated D-06 copy; segment generate 202+poll wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-immediate-cancellation/04-CONTEXT.md
@frontend/src/api/client.ts
@frontend/src/components/SegmentTable.tsx
@frontend/src/components/ConfigPanel.tsx
@frontend/src/components/ProjectScreen.tsx
@.planning/phases/04-immediate-cancellation/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client fns — segment 202+poll contract, cancelSegmentGeneration, cancelCharacterPreview</name>
  <files>frontend/src/api/client.ts</files>
  <read_first>
    - frontend/src/api/client.ts (generateSegment, triggerCharacterPreview, cancelBatchGeneration, runBatchGeneration — the fetch/parseJsonOrThrow conventions)
    - .planning/phases/04-immediate-cancellation/04-03-SUMMARY.md (the exact new endpoint paths + the segment-generate 202 status body)
  </read_first>
  <action>
    Change `generateSegment(id)` to reflect 04-03's contract: it now returns `{ status: string }` (202 accept), not a `Segment` — update its return type and its doc comment to say the caller must poll (refetch the project) for the result, mirroring `triggerCharacterPreview`. Add `cancelSegmentGeneration(id)` POSTing to `/segments/${id}/generate/cancel` and `cancelCharacterPreview(id)` POSTing to `/characters/${id}/preview/cancel`, both returning `{ status: string }` via parseJsonOrThrow, matching the existing `cancelBatchGeneration` shape. Keep everything else untouched.
  </action>
  <acceptance_criteria>
    - source: `grep -n "cancelSegmentGeneration\|cancelCharacterPreview" frontend/src/api/client.ts` finds both new functions targeting the correct paths.
    - source: `generateSegment`'s return type is `Promise<{ status: string }>` (no longer `Promise<Segment>`), with an updated doc comment about polling.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <done>Client has the two new cancel fns and generateSegment matches the async 202 contract; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Segment row — 202+poll trigger + bare-bones Stop with distinct "stopping…" state</name>
  <files>frontend/src/components/SegmentTable.tsx, frontend/src/components/ProjectScreen.tsx</files>
  <read_first>
    - frontend/src/components/SegmentTable.tsx (GeneratePlayButton — handleClick, isRowGenerating, the hidden-audio pattern; SegmentTableProps)
    - frontend/src/components/ConfigPanel.tsx lines 38-136 (CharacterPreviewRow — the existing "trigger then poll onRefresh until it lands" pattern to mirror for segments)
    - frontend/src/components/ProjectScreen.tsx (refetch, handleSegmentChange, how SegmentTable is rendered — thread refetch down)
  </read_first>
  <action>
    Thread ProjectScreen's `refetch` into SegmentTable as an `onRefresh` prop (add to SegmentTableProps and the GeneratePlayButton props). In GeneratePlayButton: after `generateSegment(id)` returns 202, do NOT expect a segment back — set the local generating flag and poll `onRefresh` on an interval (mirror CharacterPreviewRow's 1500ms interval + a 60s ceiling) until the row's `generation_status` leaves "generating"/its audio_path lands. When `isRowGenerating` is true, render a bare-bones Stop button (plain text "Stop", no color-state polish per D-04) next to/replacing the spinner. On Stop click: call `cancelSegmentGeneration(segment.id)`, set a local `isStopping` flag, and keep the control in a visibly distinct "Stopping…" state (e.g. disabled + "Stopping…" label / spinner variant distinct from the generating spinner) until a refetch shows the row settled back to a non-generating status (then clear isStopping). The three states — idle (Play/Generate), generating (spinner + Stop), stopping (distinct "Stopping…") — must all be visually distinguishable (D-03/D-05). Keep the existing play/pause-once-audio-exists behavior unchanged. Do not introduce yellow/red/green semantics (Phase 7).
  </action>
  <acceptance_criteria>
    - source: `grep -n "cancelSegmentGeneration\|Stopping" frontend/src/components/SegmentTable.tsx` finds the cancel wiring and the distinct stopping label/state.
    - source: GeneratePlayButton no longer assigns the result of `generateSegment` to a Segment / passes it to onSegmentChange; it polls via `onRefresh` instead.
    - behavior: while a row is generating, a Stop control is rendered; clicking it enters a distinct "Stopping…" state held until the row settles (not an instant flip to idle).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <done>Segment rows trigger via 202+poll, expose a bare-bones Stop, and show a distinct idle/generating/stopping tri-state; typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 3: Character-preview + batch Stop "stopping…" states and D-06 caveat copy</name>
  <files>frontend/src/components/ConfigPanel.tsx</files>
  <read_first>
    - frontend/src/components/ConfigPanel.tsx (CharacterPreviewRow's isGeneratingPreview/poll; the batch handleStop/isCancelling; the batch caveat paragraph near the Stop button)
    - .planning/phases/04-immediate-cancellation/04-CONTEXT.md D-03/D-05/D-06
  </read_first>
  <action>
    In CharacterPreviewRow: while `isGeneratingPreview` is true, render a bare-bones Stop button that calls `cancelCharacterPreview(character.id)` and enters a distinct "Stopping…" state (disabled + distinct label) held until a refetch settles the preview state — same tri-state honesty as the segment row (D-05). In the batch Generation section: the Stop button already exists — give its cancelling state a distinct "Stopping…" label (not just a bare spinner conflatable with generating), so the batch control also honors D-03's three distinct states. Rewrite the caveat paragraph under the batch Stop button (D-06): it currently tells the user the segment that is generating may still finish — that is now false. Replace it with copy stating that Stop interrupts the segment currently generating immediately. Exact wording is Claude's discretion; keep it one short line. Do not add yellow/red/green styling (Phase 7).
  </action>
  <acceptance_criteria>
    - source: `grep -n "cancelCharacterPreview\|Stopping" frontend/src/components/ConfigPanel.tsx` finds the preview Stop wiring and the distinct stopping label(s).
    - source: the batch caveat paragraph asserts immediate interruption of the in-flight segment (positive check: `grep -in "immediat\|interrupt" frontend/src/components/ConfigPanel.tsx` matches the new copy) and no longer states the current segment may still finish.
    - behavior: character-preview and batch Stop controls each show a distinct "Stopping…" transient state, not an instant flip to idle.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <done>Character-preview has a bare-bones Stop with a distinct stopping state; batch Stop shows a distinct stopping state; the D-06 caveat copy is corrected; typecheck + lint clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: D-01 true-kill end-to-end checkpoint — Stop halts the in-flight GPU call on real hardware</name>
  <action>Exercise all three Stop controls against the real TTS container and pause for human confirmation that each truly halts the in-flight GPU call (not a cosmetic UI decouple), shows the distinct stopping state, and leaves the app ready for a fresh generation, per D-01.</action>
  <what-built>Bare-bones Stop controls with a distinct "stopping…" state on segment rows, character-preview rows, and the batch control, all wired to the real backend cancel endpoints that issue a true GPU-call kill (D-01).</what-built>
  <how-to-verify>
    On the deployment VM with the real TTS container running (real GPU, not mock):
    1. Open a project, click Generate on a single segment with enough text to take a few seconds. While GPU inference is genuinely running, click Stop. Confirm: (a) inference actually halts fast — watch tts_service logs / GPU utilization, it should stop within ~a second, not run to the segment's full duration; (b) the button shows a distinct "Stopping…" state before returning to idle; (c) immediately after, you can start a new generation with no stuck "still generating" state (ROADMAP criteria 1 + 4).
    2. Repeat for a character voice preview via the config panel Stop (criterion 2).
    3. Start Generate All, and mid-run click the batch Stop. Confirm the segment currently generating is interrupted immediately (not allowed to finish), the "stopping…" state shows, and a fresh run starts cleanly afterward (criterion 3 + 4).
    4. Confirm the batch Stop caveat copy no longer claims the in-flight segment may still finish (D-06).
    If any Stop does NOT actually halt the GPU call (only decouples the UI while inference keeps running), that violates D-01 — report it rather than accepting a cosmetic stop.
  </how-to-verify>
  <resume-signal>Type "approved" once all three Stop controls truly kill the in-flight GPU call, show the distinct stopping state, and leave the app immediately ready for a new generation. Otherwise describe which path failed the D-01 true-kill test.</resume-signal>
</task>

</tasks>

<artifacts_produced>
- `frontend/src/api/client.ts`: `cancelSegmentGeneration(id)`; `cancelCharacterPreview(id)`; `generateSegment` return type changed to `Promise<{ status: string }>` (202 + poll contract).
- `frontend/src/components/SegmentTable.tsx`: `onRefresh` prop threaded through; GeneratePlayButton 202+poll trigger; bare-bones Stop button; distinct idle/generating/stopping tri-state.
- `frontend/src/components/ConfigPanel.tsx`: character-preview Stop button + stopping state; batch Stop distinct "Stopping…" state; corrected D-06 caveat copy.
- `frontend/src/components/ProjectScreen.tsx`: passes `refetch` as `onRefresh` into SegmentTable.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend→backend | Stop controls call the new cancel endpoints; segment generate now polls after a 202 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Denial of Service | poll loop after a 202 never terminating on a failed generation | low | mitigate | reuse CharacterPreviewRow's 60s ceiling on the poll interval so a failed synth stops the poll |
| T-04-12 | Spoofing | UI shows idle while backend still holds the lock (dishonest state) | medium | mitigate | "stopping…" state held until refetch/status confirms release, not an optimistic flip (D-03/D-05, Pitfall 2 UX row) |
| T-04-13 | Tampering | a false "stopped" UX while the GPU call keeps running | high | mitigate | human-verify checkpoint asserts the true-kill against real GPU utilization/logs (D-01) |
</threat_model>

<verification>
- `npm run typecheck` and `npm run lint` clean.
- Human-verify checkpoint: all three Stop paths truly kill in-flight GPU work, show the distinct stopping state, and leave the app ready for a fresh generation (D-01 + criteria 1-4).
</verification>

<success_criteria>
The user can Stop a segment, a character preview, or a batch from the UI; each shows an honest idle/generating/stopping tri-state; each triggers the real backend interrupt (proven at the checkpoint to halt the GPU call immediately); and the corrected batch caveat copy no longer misrepresents the behavior (D-04/D-05/D-06).
</success_criteria>

<output>
Create `.planning/phases/04-immediate-cancellation/04-04-SUMMARY.md` when done.
</output>
