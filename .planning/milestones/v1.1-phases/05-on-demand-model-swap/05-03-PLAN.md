---
phase: 05-on-demand-model-swap
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/components/ConfigPanel.tsx
  - frontend/src/components/SegmentTable.tsx
autonomous: false
requirements: [CFG-04, CFG-05]
must_haves:
  truths:
    - "The Config Panel shows a Model dropdown with 'Higher quality (1.7B)' / 'Faster (0.6B)' whose value is the project's server state (CFG-04, D-01)"
    - "Picking a model fires the load immediately, shows a spinner/disabled trigger during the swap, and on failure reverts to the still-resident model with an inline error (D-01/D-02)"
    - "While 0.6B is active a persistent (non-dismissible) note warns steering is unsupported, and the Voice Instructions cells are disabled (CFG-05, D-03/D-04)"
    - "After a swap the segments show their existing pending/generate state (D-05/D-06) with no new UI"
  artifacts:
    - "frontend/src/api/client.ts::setProjectModel + Project.tts_model type field"
    - "frontend/src/components/ConfigPanel.tsx Model Select + warning note + error"
    - "frontend/src/components/SegmentTable.tsx EditableTextCell disabled prop"
  key_links:
    - "Select value binds to project.tts_model (server state) so D-02 revert is automatic on refetch"
    - "generationLocked (existing lock hook) covers Generate/preview disabling during swap — no new isModelSwapping prop"
    - "ttsModel threaded from SegmentTable down to EditableTextCell for the 0.6B disabled condition"
---

<objective>
Build the two UI surfaces this phase adds: (1) the Model dropdown in `ConfigPanel.tsx` replacing the hardcoded `TTS_MODEL_DISPLAY_NAME`, with the D-01 explicit-load-on-select, D-02 failure-revert + inline error, and D-03 persistent 0.6B warning note; and (2) the D-04 disabled Voice Instructions cells in `SegmentTable.tsx` while 0.6B is active. Wire the `setProjectModel` API call and expose `tts_model` on the Project type. Then verify the whole swap end-to-end on the real deployment target (CFG-04, CFG-05).

Purpose: Completes the phase — gives the user the actual pick-a-model control and the honest steering-limitation surfaces, backed by Plans 01/02.
Output: The dropdown, the disabled cells, the API binding, and a human-verified end-to-end swap.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-on-demand-model-swap/05-CONTEXT.md
@.planning/phases/05-on-demand-model-swap/05-UI-SPEC.md
@.planning/phases/05-on-demand-model-swap/05-PATTERNS.md
@frontend/src/api/client.ts
</context>

## Artifacts this phase produces (Plan 03)

- `tts_model: string` on the `Project` TypeScript interface (`frontend/src/api/client.ts`).
- `setProjectModel(id, model_id): Promise<Project>` API mutation (POSTs `/projects/{id}/model`).
- Model `Select` + D-03 warning `<p>` + D-02 error `<p>` in `ConfigPanel.tsx` (replaces `TTS_MODEL_DISPLAY_NAME` constant and its `ConfigField` row).
- `isSwapping` / `swapError` local state + `handleModelChange` in `ConfigPanel.tsx`.
- `ttsModel` prop threaded into `SegmentTable` then `EditableTextCell`, driving the `disabled`/`title` on the Voice Instructions `Textarea` (D-04).

<tasks>

<task type="auto">
  <name>Task 1: Add setProjectModel API + Model Select with swap/warn/error UX in ConfigPanel</name>
  <files>frontend/src/api/client.ts, frontend/src/components/ConfigPanel.tsx</files>
  <read_first>
    - frontend/src/api/client.ts (the Project interface at line 45; the mutation pattern e.g. patchSegment/runBatchGeneration; the errorMessage helper at line 17)
    - frontend/src/components/ConfigPanel.tsx (the TTS_MODEL_DISPLAY_NAME constant and its ConfigField row; CharacterPreviewRow's isTriggering/error/setError, the errorMessage(err, fallback) helper, and the destructive inline-error paragraph pattern; the existing generationLocked prop; the Select import set already used by BulkReassignToolbar)
    - .planning/phases/05-on-demand-model-swap/05-UI-SPEC.md §"Component Contracts §1" (exact JSX/Tailwind/copy — binding), §"Copywriting Contract" (verbatim option labels, warning copy, error copy), §"Color" (D-03 note is text-muted-foreground, NOT destructive)
  </read_first>
  <action>
    In `frontend/src/api/client.ts`: add `tts_model: string` to the `Project` interface. Add `setProjectModel(id, model_id)` returning `Promise<Project>`, mirroring the existing mutation helpers — POST to `/projects/{id}/model` with a JSON body carrying `model_id`, check `response.ok` the same way the siblings do, return the parsed `Project`.

    In `frontend/src/components/ConfigPanel.tsx`: delete the `TTS_MODEL_DISPLAY_NAME` constant and its `ConfigField label="Model"` row. Replace with the UI-SPEC §1 block verbatim — a `flex flex-col gap-1` wrapper containing the `Select` (value bound to `project.tts_model`, `onValueChange={handleModelChange}`, `disabled={isSwapping}`, two `SelectItem`s: value `1.7b` label "Higher quality (1.7B)", value `0.6b` label "Faster (0.6B)"), the swap spinner label (`Loader2 size-4 animate-spin` + "Switching model…" while `isSwapping`, else `SelectValue`), the D-03 conditional warning paragraph `flex items-center gap-1 text-xs text-muted-foreground` with a `TriangleAlert size-3 shrink-0` icon (rendered only when `project.tts_model === "0.6b"`, verbatim warning copy from the Copywriting Contract — muted tone, NOT destructive per the Color decision), and the D-02 conditional `swapError` paragraph `text-xs text-destructive` with `role="alert"`.

    Add local state `isSwapping`/`swapError` and `handleModelChange(next)`: set `isSwapping(true)` and clear `swapError`; `await setProjectModel(project.id, next)` then `onRefresh()`; on error set `swapError` via `errorMessage(err, fallback)` where the fallback uses the D-02 copy "Couldn't switch to {attempted}. Still using {resident}." with human labels; `isSwapping(false)` in finally — mirroring CharacterPreviewRow's try/catch/finally shape. D-01 fires the load immediately on select; D-02 revert is automatic because the Select value is server state re-read via `onRefresh`, never optimistic local state. Do NOT add an `isModelSwapping` prop through ConfigPanel/SegmentTable — the existing `generationLocked` prop already disables Generate All / per-row / preview controls during the swap because model-load claims the same single-flight backend lock (UI-SPEC §1 explicit instruction).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && grep -q "setProjectModel" src/api/client.ts && grep -q "tts_model" src/api/client.ts && grep -q "Higher quality (1.7B)" src/components/ConfigPanel.tsx && grep -q "Faster (0.6B)" src/components/ConfigPanel.tsx && ! grep -q "TTS_MODEL_DISPLAY_NAME" src/components/ConfigPanel.tsx && echo "config-panel-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `Project` type has `tts_model`; `setProjectModel` POSTs to `/projects/{id}/model`.
    - Source: `TTS_MODEL_DISPLAY_NAME` is gone; the Select shows both verbatim option labels; the D-03 note uses `text-muted-foreground` (not destructive); the D-02 error uses `text-destructive` + `role="alert"`.
    - Behavior: `npx tsc --noEmit` passes; no new `isModelSwapping` prop introduced.
  </acceptance_criteria>
  <done>The Config Panel drives a real per-project model swap with immediate load, spinner, automatic revert-on-failure with an inline error, and a persistent 0.6B steering warning.</done>
</task>

<task type="auto">
  <name>Task 2: Disable Voice Instructions cells while 0.6B is active (D-04)</name>
  <files>frontend/src/components/SegmentTable.tsx</files>
  <read_first>
    - frontend/src/components/SegmentTable.tsx (EditableTextCell at line 318 — its props and the Textarea; the EditableTextCell call sites around line 490; SegmentTable's own props to see where project/ttsModel enters)
    - .planning/phases/05-on-demand-model-swap/05-UI-SPEC.md §"Component Contracts §2" (exact disabled/title diff, reuses Textarea's existing disabled: variant — zero new CSS)
  </read_first>
  <action>
    Thread the project's model id into `EditableTextCell` as a `ttsModel: string` prop (smallest-diff addition per UI-SPEC §2 — add it to SegmentTable's props if not already present and pass it down at the two EditableTextCell call sites alongside `segment`/`field`/`label`/`onSegmentChange`). On the `Textarea`, add `disabled={field === "voice_instructions" && ttsModel === "0.6b"}` and a `title` attribute set to "Voice instructions have no effect while Faster (0.6B) is active." under the same condition (else `undefined`), exactly as UI-SPEC §2 specifies. Add no new CSS — the existing `disabled:` variant on `ui/textarea.tsx` covers the visual treatment. Only the `voice_instructions` field is gated; the Text column stays editable.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && grep -q "ttsModel" src/components/SegmentTable.tsx && grep -q "Voice instructions have no effect while Faster (0.6B) is active" src/components/SegmentTable.tsx && echo "segment-table-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Source: the Voice Instructions `Textarea` is `disabled` and carries the exact tooltip copy only when `ttsModel === "0.6b"`; the Text field is unaffected.
    - Behavior: `npx tsc --noEmit` passes; no new CSS added.
  </acceptance_criteria>
  <done>When 0.6B is active, the per-segment Voice Instructions cells are visibly disabled with an explanatory tooltip, reinforcing that edits have no effect (D-04).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the full model swap end-to-end on the deploy target</name>
  <action>Pause for human verification. Ensure the frontend is built/served and both backend + tts_service containers are running the newly-implemented code on the deploy target, then walk the human through the steps in how-to-verify and wait for the resume-signal before completing the plan.</action>
  <what-built>The full on-demand model swap, end-to-end on the real RX 9070 XT deployment target: the Config Panel Model dropdown (Plan 03), the backend swap endpoint + segment/preview invalidation (Plan 02), and the tts_service ensure_loaded swap machinery (Plan 01).</what-built>
  <how-to-verify>
    1. Open the app (Tailscale URL) and open a project that already has at least one generated segment and one character preview.
    2. In the Config Panel, switch the Model dropdown from "Higher quality (1.7B)" to "Faster (0.6B)". Confirm: the trigger shows the "Switching model…" spinner and is disabled for the swap duration (tens of seconds), and Generate All / per-row / preview controls are disabled during that window.
    3. After the swap completes, confirm: a persistent muted warning note appears under the dropdown about 0.6B not supporting voice instructions; every segment's Voice Instructions cell is grayed out/disabled with the tooltip; every segment reverted to the pending "Generate Preview" state; the previously-generated character preview is also cleared (no stale play).
    4. Regenerate one segment and one character preview under 0.6B — confirm fresh audio is produced (no stale cross-model cache hit).
    5. Switch back to "Higher quality (1.7B)"; confirm the warning note and disabled cells clear, and Voice Instructions become editable again.
    6. (VRAM sanity — automated already-passing in Plan 01, spot-check here) In the tts_service container logs, confirm the `mem_get_info` before/after log lines around the swap show stable free VRAM.
    7. Simulate a failure if feasible (e.g. temporarily point at a bad checkpoint id via a direct API call, or observe a real OOM): confirm the dropdown reverts to the still-resident model and an inline red error appears — the project is never left in a "no model" limbo (D-02).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → backend | The dropdown only ever emits the two fixed `MODEL_CHOICES` ids; the backend (Plan 02) is the enforcing boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | client sends a model_id outside the two fixed options | low | mitigate | The dropdown is a fixed 2-option `Select` (no free text); any crafted value is still rejected 422 by the backend allowlist (Plan 02 T-05-01) — client is not a trust boundary |
| T-05-02 | Denial of Service | user spam-clicks the dropdown during a swap | low | mitigate | Trigger is `disabled={isSwapping}` for the swap duration; backend single-flight lock returns 409 for any concurrent claim regardless |
| T-05-SC | Tampering (supply chain) | new frontend dependency | low | accept | No new npm package — all shadcn primitives (`Select`, `Textarea`, icons) already installed (UI-SPEC Registry Safety) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes.
- The Model Select shows both verbatim labels; D-03 note is muted, D-02 error is destructive+role=alert; `TTS_MODEL_DISPLAY_NAME` removed.
- Voice Instructions cells disabled + tooltip only under 0.6B; Text unaffected.
- Human-verify checkpoint approved: real swap on the deploy target shows spinner, invalidation of segments + previews, no stale cross-model audio, muted warning, disabled cells, and D-02 revert on failure.
</verification>

<success_criteria>
A user can pick between the two models per project from the Config Panel; the swap loads immediately with a clear in-progress state, warns and disables steering surfaces on 0.6B, invalidates prior audio so no stale cross-model audio is served, and safely reverts on failure — all verified live on the RX 9070 XT target.
</success_criteria>

<output>
Create `.planning/phases/05-on-demand-model-swap/05-03-SUMMARY.md` when done.
</output>
