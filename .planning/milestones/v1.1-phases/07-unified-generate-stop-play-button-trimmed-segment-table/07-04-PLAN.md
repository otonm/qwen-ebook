---
phase: 07-unified-generate-stop-play-button-trimmed-segment-table
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/src/components/CharacterCard.tsx
  - frontend/src/components/CastWizard.tsx
autonomous: true
requirements: [GEN-10, GEN-12]
must_haves:
  truths:
    - "CharacterCard's wizard preview control is one <GenerateStopPlayButton size=\"sm\"> that now gains a working Stop control for the first time (D-01) — amber 'Generate Preview' with no preview, red 'Stop Generation'/'Stopping…' while generating, green 'Play' once preview_audio_path exists (GEN-10)."
    - "CharacterCard's weaker local generate state (no Stop button, hardcoded 60000ms ceiling) is replaced wholesale by useGenerateStopPlay — it now uses the shared GENERATION_POLL_CEILING_MS ceiling, not its own 60s value (RESEARCH Pitfall 3)."
    - "Everything else in CharacterCard stays untouched (D-02): the name Input, the Preset Select, the Voice Instructions Textarea, the 'Voice assigned' Badge, and the merge Dialog/button — the surrounding row layout is not reshaped."
    - "CastWizard's outer two-column flex container gains xl:items-start so the xl:w-[420px] character-card column sizes to its own content height instead of stretching to the SegmentPreview column's height; SegmentPreview continues rendering at its own natural height (D-05)."
    - "GEN-12: editing a character in the wizard clears preview_audio_path server-side and the CharacterCard button reverts to amber idle via the hook's reactive prop read — no separate status indicator."
    - "SegmentPreview.tsx is NOT modified — it stays a read-only, text-only preview with no per-row controls, no aggregate Generate All/Stop, no audio (D-03: 'serves only as a preview')."
    - "E4 overflow: xl:items-start stops the card column stretching at zero, one, or many characters — each column sizes to its own content independently (UI-SPEC UI Considerations E4)."
    - "E4 long-text: card content (names, voice instructions) wraps inside the fixed xl:w-[420px] column exactly as today — the one-class layout fix alters no text container (UI-SPEC UI Considerations E4)."
  artifacts:
    - "frontend/src/components/CharacterCard.tsx (modified — button block swapped to GenerateStopPlayButton+useGenerateStopPlay; local 60s poll ceiling removed)"
    - "frontend/src/components/CastWizard.tsx (modified — one class xl:items-start added to the outer flex container)"
  key_links:
    - "CharacterCard computes hasAudio from character.preview_audio_path, isExternallyGenerating from its existing 'is this character previewing' signal, and dispatches to triggerCharacterPreview/cancelCharacterPreview."
    - "generationLocked continues to arrive as a prop and gate the idle button; the local hardcoded 60000ms timeout is deleted in favor of the hook's GENERATION_POLL_CEILING_MS."
  prohibitions:
    - "SegmentPreview.tsx must not gain any generate/stop/audio capability — it stays a read-only text preview (D-03, user explicit)."
    - "CharacterCard's surrounding row layout (name Input, Preset Select, Voice Instructions Textarea, merge Dialog) must not be reshaped — only the generate/stop/play control itself is replaced (D-02)."
    - "CastWizard's inner column classes (xl:w-[420px] xl:flex-none) must not change and no independent scroll panes added — the fix is the single xl:items-start class on the outer container (D-05)."
---

<objective>
Fold `CharacterCard.tsx`'s wizard-side preview button — the 4th independently-coded generate/play implementation, which today has no Stop control at all — into the shared `<GenerateStopPlayButton>` + `useGenerateStopPlay`, giving it a working Stop for the first time (D-01/D-02, GEN-10). Fix `CastWizard.tsx`'s stretched character-card column with one Tailwind class (D-05). `SegmentPreview.tsx` is deliberately left untouched (D-03).

Purpose: The last of the four sites unified, plus the folded-todo layout fix.
Output: Modified `CharacterCard.tsx` and `CastWizard.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-UI-SPEC.md
@.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-RESEARCH.md
@.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-PATTERNS.md
@.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Swap CharacterCard's button block to the shared component, gaining a Stop control (D-01/D-02, GEN-10)</name>
  <files>frontend/src/components/CharacterCard.tsx</files>
  <read_first>
    - frontend/src/components/CharacterCard.tsx — the full file: the hasPreview ? icon-button : text-'Generate'-button branch (lines ~209-239), the isWaitingForPreview local 60s poll ceiling (lines ~80-88, hardcoded 60000), handleGenerate, togglePlayback, the hidden <audio> (lines ~243-251), and the untouched name Input/Preset Select/Voice Instructions Textarea/Badge/merge Dialog
    - frontend/src/components/ConfigPanel.tsx — CharacterPreviewRow (the more mature sibling): the correct isTriggeringPreview/hasPreview split to model the swap on (RESEARCH Pitfall 3)
    - frontend/src/components/GenerateStopPlayButton.tsx — prop contract (Plan 01)
    - frontend/src/hooks/useGenerateStopPlay.ts — hook signature (Plan 01)
    - frontend/src/api/client.ts — triggerCharacterPreview, cancelCharacterPreview, previewUrl, GENERATION_POLL_CEILING_MS
  </read_first>
  <action>Replace the `hasPreview ? <icon button> : <text 'Generate' button>` branch with ONE `<GenerateStopPlayButton size="sm" ... />` fed by `useGenerateStopPlay({ hasAudio: Boolean(character.preview_audio_path), isExternallyGenerating: <existing previewing signal>, poll: true, onGenerate: () => triggerCharacterPreview(character.id), onStop: () => cancelCharacterPreview(character.id), onRefresh })`. This gives CharacterCard a real Stop control for the first time (D-01). Set `subjectLabel={\`preview for ${character.name}\`}`, `disabled={generationLocked && status === "idle"}`, `disabledReason="Another generation is already running."`, `isPlaying` from the existing playback toggle, `onTogglePlay` → togglePlayback. Delete the local hardcoded 60-second poll-ceiling timeout and its isWaitingForPreview scaffolding (the exact literal value is named in read_first) — the hook owns poll/settle at GENERATION_POLL_CEILING_MS now (RESEARCH Pitfall 3: replace wholesale, do not patch). Keep the hidden `<audio src={previewUrl(...)}>` and its isPlaying wiring. Add a per-row error paragraph fed by the hook's `error` if one is not already present. Do NOT touch the name Input, Preset Select, Voice Instructions Textarea, 'Voice assigned' Badge, or the merge Dialog/button, and do NOT reshape the row layout (D-02).</action>
  <verify>
    <automated>cd frontend && grep -q 'GenerateStopPlayButton' src/components/CharacterCard.tsx && grep -q 'useGenerateStopPlay' src/components/CharacterCard.tsx && test $(grep -c '60000' src/components/CharacterCard.tsx) -eq 0 && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - CharacterCard renders exactly one GenerateStopPlayButton driven by useGenerateStopPlay (gains a Stop control)
    - The hardcoded 60000ms ceiling is gone (`grep -c '60000'` returns 0); the shared GENERATION_POLL_CEILING_MS is used via the hook
    - hasAudio maps from character.preview_audio_path; subjectLabel interpolates character.name
    - The name Input, Preset Select, Voice Instructions Textarea, Badge, and merge Dialog remain unchanged (git diff shows those blocks untouched)
    - `npm run typecheck` and `npm run lint` pass
  </acceptance_criteria>
  <done>CharacterCard's preview control is one amber/red/green button with a working Stop, using the shared ceiling; everything else in the card is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CastWizard's stretched character-card column (D-05)</name>
  <files>frontend/src/components/CastWizard.tsx</files>
  <read_first>
    - frontend/src/components/CastWizard.tsx — the outer two-column flex container (line ~108, current class `flex flex-col gap-8 xl:flex-row xl:gap-8`) and the inner xl:w-[420px] xl:flex-none column (line ~109) and the SegmentPreview flex-1 column (line ~124)
    - .planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-UI-SPEC.md — Component Contracts §5 (the exact one-class edit)
  </read_first>
  <action>Add `xl:items-start` to the outer flex container class so it becomes `flex flex-col gap-8 xl:flex-row xl:items-start xl:gap-8`. This overrides the flex row's default items-stretch, so the xl:w-[420px] xl:flex-none character-card column sizes to its own content height instead of stretching to match SegmentPreview's height. Change NOTHING else — do not touch the inner column classes, do not add independent scroll panes (rejected alternative, D-05), and do not modify SegmentPreview.tsx (D-03).</action>
  <verify>
    <automated>cd frontend && grep -q 'xl:items-start' src/components/CastWizard.tsx && test $(git diff --stat src/components/SegmentPreview.tsx 2>/dev/null | wc -l) -eq 0 && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The outer flex container includes `xl:items-start` (`grep -c 'xl:items-start' src/components/CastWizard.tsx` >= 1)
    - The inner column classes (xl:w-[420px] xl:flex-none) are unchanged
    - SegmentPreview.tsx is NOT modified (git diff empty for that file)
    - `npm run typecheck`, `npm run lint`, and `npm run build` pass
  </acceptance_criteria>
  <done>The character-card column sizes to its content; SegmentPreview is untouched; the full frontend build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend HTTP | CharacterCard's preview button dispatches to existing trigger/cancel character-preview endpoints; CastWizard change is layout-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Denial of Service | CharacterCard's new Stop / double-trigger | low | mitigate | Replacing the weak local state wholesale with the shared hook adds the missing double-cancel guard (disabled while stopping) and a proper settle guard, preventing the double-POST/stuck-Stopping race the naive port risks (RESEARCH Pitfall 3) |
| T-07-08 | Tampering | CastWizard layout class | low | accept | Pure presentational Tailwind class change — no data or endpoint surface |
</threat_model>

<verification>
- `cd frontend && npm run typecheck && npm run lint && npm run build` all pass
- CharacterCard uses the shared component + hook; 60000ms ceiling removed
- CastWizard has xl:items-start; SegmentPreview.tsx unchanged
</verification>

<success_criteria>
GEN-10: CharacterCard's preview control follows the yellow/red/green pattern and has a working Stop (D-01). D-02: only the button is swapped, layout untouched. D-05: the card column sizes to content. D-03: SegmentPreview stays a read-only preview.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

- No new exported symbols — both files refactored in place. `CharacterCard.tsx` consumes the shared `useGenerateStopPlay` + `GenerateStopPlayButton` (Plan 01) and drops its local 60s ceiling; `CastWizard.tsx` gains one Tailwind class. `SegmentPreview.tsx` intentionally unchanged (D-03).
</artifacts_produced>

<output>
Create `.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-04-SUMMARY.md` when done
</output>
