---
phase: 07-unified-generate-stop-play-button-trimmed-segment-table
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/src/components/SegmentTable.tsx
autonomous: true
requirements: [GEN-09, GEN-12, TBL-05]
must_haves:
  truths:
    - "Each segment row renders exactly one <GenerateStopPlayButton size=\"sm\"> (no second adjacent Stop button): amber 'Generate Preview' when generation_status is idle/pending and no audio, red 'Stop Generation'/'Stopping…' while generating, green 'Play' once audio_path exists (GEN-09)."
    - "The SegmentTable status display column, the STATUS_BADGE map, and the StatusBadge function are all deleted, AND a Voice Instructions editable column is added between narrator and text; the columns array has exactly 5 entries: select, narrator, voice_instructions, text, controls — 3 editable content columns reading Narrator | Voice Instructions | Text (TBL-05, D-07)."
    - "TBL-05 editable: the Voice Instructions column reuses the generic EditableTextCell with field=\"voice_instructions\" and label \"Voice Instructions\", committing via patchSegment on blur; editing it resets generation_status to pending server-side and reverts the row button to amber idle via the SAME GEN-12 path as text/narrator edits — no new cell component or backend change."
    - "The now-dead Badge, AlertCircle, CheckCircle2, Clock imports are removed; Loader2 is kept (still used by the bulk-reassign toolbar) — no dead imports remain (RESEARCH Pitfall 4)."
    - "GEN-09 adjacency: a segment that is both generating and already has audio_path shows red generating (precedence generating > ready), never green Play, because the shared hook derives status in stopping > generating > ready > idle order."
    - "GEN-09 empty: a segment with no audio_path and a non-generating status renders the amber idle 'Generate Preview' state (single-segment / freshly-created row)."
    - "GEN-09 ordering: removing the Status column does not reorder segment rows — row order is unchanged from Phase 3's table ordering."
    - "GEN-12 empty: editing a segment (clearing its audio server-side, generation_status reset to pending) reverts the button to amber idle via the hook's reactive read of props — no separate status badge survives anywhere near the button."
    - "GEN-12 encoding: editing a segment's text with any Unicode content triggers the same server-side generation_status='pending' reset and the button reverts to amber idle (state read off props, not text length)."
    - "E2 empty/loading/error/populated/partial/overflow/zero-one-many: the table's Phase 3 empty, skeleton, per-row error paragraph, and horizontal-wrap behaviors are unchanged; per-row in-flight state is now the button's red Loader2 spin instead of a Status badge; mixed generated/ungenerated rows are distinguished by button color alone (UI-SPEC UI Considerations E2)."
    - "TBL-05 empty: with zero segments the table still renders its 5 columns (select, narrator, voice_instructions, text, controls) with no Status column (Phase 3 empty rendering unaffected)."
    - "TBL-05 encoding: the deleted STATUS_BADGE/StatusBadge code and its Badge/AlertCircle/CheckCircle2/Clock imports are fully removed, not merely unreferenced."
  artifacts:
    - "frontend/src/components/SegmentTable.tsx (modified — GeneratePlayButton body replaced with hook+shared-component wrapper; Status column and badge code deleted)"
  key_links:
    - "The controls column cell computes hasAudio from segment.audio_path and isExternallyGenerating from segment.generation_status === 'generating', feeds useGenerateStopPlay, renders GenerateStopPlayButton."
    - "generationLocked continues to arrive as a prop and gate the idle button's disabled state — not re-derived."
  prohibitions:
    - "No status text, icon, or badge may render near the segment button beyond the existing error paragraph — the button color/label is the single visual source of truth (GEN-12)."
    - "The controls column button must not re-derive the generation lock via useGenerationLock() — it consumes the existing generationLocked prop."
---

<objective>
Swap `SegmentTable.tsx`'s hand-rolled `GeneratePlayButton` for the shared `<GenerateStopPlayButton>` + `useGenerateStopPlay` (GEN-09), delete the separate Status column, its `STATUS_BADGE`/`StatusBadge` code, and the now-dead imports (D-07), and add the Voice Instructions editable column so the table shows exactly 3 editable content columns — Narrator | Voice Instructions | Text (TBL-05). The button becomes the single visual source of truth for per-segment generation state (GEN-12).

Purpose: One consistent yellow/red/green control per segment row and a table with the required Narrator | Voice Instructions | Text editable columns (Status column gone).
Output: Modified `SegmentTable.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-UI-SPEC.md
@.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-PATTERNS.md
@.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the Status column + StatusBadge/STATUS_BADGE + dead imports, and add the Voice Instructions column (TBL-05)</name>
  <files>frontend/src/components/SegmentTable.tsx</files>
  <read_first>
    - frontend/src/components/SegmentTable.tsx — read the full file: the StatusBadge function + STATUS_BADGE map (lines ~60-86), the generic EditableTextCell (lines ~318-366 — note its `field` prop already accepts "voice_instructions" | "text" and commits via patchSegment on blur), the narrator/text/status columnHelper.display entries and the columns array (lines ~456-516), and the top import block (lines ~1-30) to identify Badge/AlertCircle/CheckCircle2/Clock usage
    - .planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-UI-SPEC.md — Component Contracts §2 and Copywriting Contract (TBL-05: no replacement status copy)
  </read_first>
  <action>Three edits to the columns array + import block, no new components. (1) Delete the status-badge component function and its status-to-badge lookup map entirely (both named in read_first, lines ~60-86). (2) Delete the `status` `columnHelper.display({ id: "status", ... })` entry from the columns array. (3) Add a new `columnHelper.display({ id: "voice_instructions", header: "Voice Instructions", ... })` entry positioned BETWEEN the narrator and text entries, whose cell renders `<EditableTextCell segment={row.original} field="voice_instructions" label="Voice Instructions" onSegmentChange={onSegmentChange} />` — reuse the existing generic EditableTextCell verbatim (its `field` prop already supports "voice_instructions" and its onBlur calls patchSegment, so editing it triggers the identical GEN-12 pending-reset/amber-revert path as text/narrator edits; no new cell and no backend work). After these edits the columns array is exactly 5 entries in order: select, narrator, voice_instructions, text, controls. Remove the now-unused imports `Badge`, `AlertCircle`, `CheckCircle2`, `Clock`. Before removing `Loader2`, grep-confirm it is still referenced elsewhere (the bulk-reassign toolbar and/or the button) and KEEP it. No replacement status copy renders anywhere — GEN-12 makes the button the single source of truth.</action>
  <verify>
    <automated>cd frontend && test $(grep -c 'STATUS_BADGE' src/components/SegmentTable.tsx) -eq 0 && test $(grep -c 'function StatusBadge' src/components/SegmentTable.tsx) -eq 0 && test $(grep -cE 'AlertCircle|CheckCircle2' src/components/SegmentTable.tsx) -eq 0 && grep -q 'field="voice_instructions"' src/components/SegmentTable.tsx && grep -q 'id: "voice_instructions"' src/components/SegmentTable.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'STATUS_BADGE' src/components/SegmentTable.tsx` returns 0
    - `grep -c 'id: "status"' src/components/SegmentTable.tsx` returns 0
    - `grep -cE 'AlertCircle|CheckCircle2|Clock' src/components/SegmentTable.tsx` returns 0 (Clock removed unless still referenced — verify)
    - `grep -c 'field="voice_instructions"' src/components/SegmentTable.tsx` returns 1 and the voice_instructions column sits between narrator and text
    - The columns array has exactly 5 entries in order: select, narrator, voice_instructions, text, controls
    - Loader2 import remains (still referenced by toolbar/button)
    - `npm run typecheck` and `npm run lint` pass (no unused-import errors)
  </acceptance_criteria>
  <done>The Status column and badge code are gone, the Voice Instructions editable column is added between Narrator and Text, and the table typechecks and lints clean with a 5-entry columns array (3 editable content columns: Narrator | Voice Instructions | Text).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace GeneratePlayButton with the shared component wired via useGenerateStopPlay (GEN-09)</name>
  <files>frontend/src/components/SegmentTable.tsx</files>
  <read_first>
    - frontend/src/components/SegmentTable.tsx — the GeneratePlayButton function (lines ~99-269): its generate/stop handlers, hidden <audio> element, isPlaying toggle, error paragraph, and generationLocked usage
    - frontend/src/components/GenerateStopPlayButton.tsx — the new component's prop contract (from Plan 01)
    - frontend/src/hooks/useGenerateStopPlay.ts — the hook signature (from Plan 01)
    - frontend/src/api/client.ts — generateSegment, cancelSegmentGeneration, segmentAudioUrl, errorMessage signatures
  </read_first>
  <action>Replace GeneratePlayButton's internal two-button JSX with a thin wrapper: call `useGenerateStopPlay({ hasAudio: Boolean(segment.audio_path), isExternallyGenerating: segment.generation_status === "generating", poll: true, onGenerate: () => generateSegment(segment.id), onStop: () => cancelSegmentGeneration(segment.id), onRefresh })` and render one `<GenerateStopPlayButton size="sm" status={status} isPlaying={isPlaying} disabled={generationLocked && status === "idle"} disabledReason="Another generation is already running." subjectLabel={\`audio for segment ${segment.order + 1}\`} onGenerate={handleGenerate} onStop={handleStop} onTogglePlay={togglePlayback} />`. Keep the existing hidden `<audio>` element (segmentAudioUrl) and its isPlaying onPlay/onPause/onEnded wiring, and keep the per-row error paragraph `<p className="text-xs text-destructive" role="alert">{error}</p>` below the button using the hook's `error`. The controls column cell renders this wrapper. Reuse the exact existing aria-label wording via subjectLabel. Do NOT re-derive the generation lock.</action>
  <verify>
    <automated>cd frontend && grep -q 'GenerateStopPlayButton' src/components/SegmentTable.tsx && grep -q 'useGenerateStopPlay' src/components/SegmentTable.tsx && test $(grep -c 'useGenerationLock' src/components/SegmentTable.tsx) -eq 0 && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - SegmentTable imports and renders GenerateStopPlayButton fed by useGenerateStopPlay
    - The controls cell renders exactly one button (no separate Stop button JSX remains)
    - hasAudio maps from segment.audio_path; isExternallyGenerating from segment.generation_status === "generating"
    - generationLocked is consumed as a prop (`grep -c useGenerationLock` returns 0)
    - The per-row error paragraph and hidden <audio>/isPlaying playback are preserved
    - `npm run typecheck`, `npm run lint`, and `npm run build` pass
  </acceptance_criteria>
  <done>Each segment row shows one amber/red/green button driven by the shared hook+component; the full frontend build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend HTTP | Segment button dispatches to existing generateSegment / cancelSegmentGeneration endpoints (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering | SegmentTable controls cell | low | accept | Only re-routes existing, already-validated segment generate/cancel calls through one button instead of two; no new input or endpoint |
| T-07-04 | Information Disclosure | deleted Status column | low | accept | Removing a read-only status badge exposes no data; generation state still readable via button color (single-user Tailscale boundary) |
</threat_model>

<verification>
- `cd frontend && npm run typecheck && npm run lint && npm run build` all pass
- Status column, STATUS_BADGE, StatusBadge, and dead imports removed (greps return 0)
- Voice Instructions editable column added between Narrator and Text (columns array = select, narrator, voice_instructions, text, controls)
- Each segment row renders one GenerateStopPlayButton
</verification>

<success_criteria>
GEN-09: each segment row is a single yellow/red/green button. TBL-05: the table shows exactly 3 editable content columns — Narrator | Voice Instructions | Text (columns array = select, narrator, voice_instructions, text, controls) — with the Status column and its code fully deleted and the Voice Instructions column added via the generic EditableTextCell. GEN-12: the button color/label is the only per-row state indicator, and editing Voice Instructions reverts it to amber via the same PATCH path as text/narrator.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- No new symbols — `SegmentTable.tsx` is refactored in place. `GeneratePlayButton`'s internals are replaced by a wrapper over the shared hook/component; `StatusBadge`/`STATUS_BADGE` are deleted (removed symbols); a `voice_instructions` column entry is added to the columns array reusing the existing generic `EditableTextCell` (no new component).
</artifacts_produced>

<output>
Create `.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-02-SUMMARY.md` when done
</output>
