---
phase: 07-unified-generate-stop-play-button-trimmed-segment-table
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/hooks/useGenerateStopPlay.ts
  - frontend/src/components/GenerateStopPlayButton.tsx
autonomous: true
requirements: [GEN-12]
must_haves:
  truths:
    - "GspStatus is derived by useGenerateStopPlay in the exact precedence order stopping > generating > ready > idle, so a row that is both generating and already has audio renders generating, never ready (D-08, GEN-12 reactive source of truth)."
    - "GenerateStopPlayButton renders exactly one <Button> per site with STATE_CLASSES amber/red/green backgrounds and STATE_LABEL 'Generate Preview' / 'Stop Generation' / 'Stopping…' / 'Play' per UI-SPEC Component Contracts §1 (D-06)."
    - "When hasAudio is false and no generation is in flight, useGenerateStopPlay returns status 'idle' — the reactive revert-to-yellow mechanism GEN-12 depends on (edit clears server audio → hasAudio false → idle)."
    - "outputUrl(projectId) returns the exact same route string as downloadUrl (/projects/{id}/download) — no new backend endpoint (GEN-11 reuse of Phase 6 route)."
    - "E1 long-text: button labels are a fixed hardcoded set (never user-generated), so the button auto-sizes to its label via Button's intrinsic width with no truncation/wrap handling (UI-SPEC UI Considerations E1)."
  artifacts:
    - "frontend/src/components/GenerateStopPlayButton.tsx (new — presentational component + GspStatus type)"
    - "frontend/src/hooks/useGenerateStopPlay.ts (new — shared poll/settle state-machine hook)"
    - "frontend/src/api/client.ts (modified — outputUrl helper added)"
  key_links:
    - "useGenerateStopPlay imports GENERATION_POLL_CEILING_MS and errorMessage from @/api/client (already exported)."
    - "GenerateStopPlayButton passes STATE_CLASSES through Button's className prop; tailwind-merge (cn) lets the state bg-* win over Button's default variant."
  prohibitions:
    - "The amber/red/green state colors must not be promoted to app-wide --warning/--success CSS custom properties in index.css — they stay scoped to GenerateStopPlayButton only."
    - "GenerateStopPlayButton and useGenerateStopPlay must not call useGenerationLock() themselves — the lock arrives as a disabled/generationLocked prop (avoids N redundant 1.5s polls)."
    - "No 4th 'queued' button state — queued folds into the amber idle state."
---

<objective>
Extract the shared foundation this phase's four call sites all consume: one presentational `<GenerateStopPlayButton>` component (per the approved UI-SPEC) and one `useGenerateStopPlay` hook that owns the poll/settle/error state machine currently duplicated across three sites, plus a thin `outputUrl` helper for the batch Play state.

Purpose: Collapse ~300 lines of near-identical generate/stop/play logic into one hook + one component so the 4 consumer plans (Wave 2) become thin swaps. The hook's status derivation is the reactive single-source-of-truth mechanism GEN-12 requires.

Output: `GenerateStopPlayButton.tsx`, `useGenerateStopPlay.ts`, and an `outputUrl` addition to `client.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-UI-SPEC.md
@.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-RESEARCH.md
@.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add outputUrl helper to api/client.ts</name>
  <files>frontend/src/api/client.ts</files>
  <read_first>
    - frontend/src/api/client.ts — read the existing downloadUrl function and confirm GENERATION_POLL_CEILING_MS and errorMessage are exported (the new hook imports both)
  </read_first>
  <action>Add an exported `outputUrl(projectId: string): string` function immediately adjacent to the existing `downloadUrl`, returning the identical route template `/projects/${projectId}/download`. It is a pure string builder over a server-issued project id — same route Phase 6 already serves per-format via FileResponse (audio/flac, audio/mpeg, audio/ogg with Range support). Do NOT add a new backend endpoint. Use f-string-equivalent template literal, matching downloadUrl verbatim in shape.</action>
  <verify>
    <automated>cd frontend && grep -q 'export function outputUrl' src/api/client.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function outputUrl' frontend/src/api/client.ts` returns 1
    - outputUrl returns the same route string as downloadUrl (`/projects/{id}/download`)
    - `npm run typecheck` passes with no new errors
  </acceptance_criteria>
  <done>outputUrl is exported from client.ts and typechecks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create useGenerateStopPlay hook</name>
  <files>frontend/src/hooks/useGenerateStopPlay.ts</files>
  <read_first>
    - frontend/src/components/SegmentTable.tsx — GeneratePlayButton (lines ~99-269): the hasObservedGeneratingRef stale-settle guard and generate/stop handlers
    - frontend/src/components/ConfigPanel.tsx — CharacterPreviewRow (lines ~55-204): the poll-until-settled effect with GENERATION_POLL_CEILING_MS ceiling-timeout error recovery
    - .planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-RESEARCH.md — Pattern 1 (lines 156-250): the complete drafted hook shape to use as the starting point
    - frontend/src/api/client.ts — confirm GENERATION_POLL_CEILING_MS and errorMessage import names
  </read_first>
  <action>Create the hook taking options `{ hasAudio, isExternallyGenerating, poll = true, onGenerate, onStop, onRefresh }` and returning `{ status, error, handleGenerate, handleStop }`. Own local state `isGenerating`, `isStopping`, `error`, and a `hasObservedGeneratingRef`. Derive `status: GspStatus` in the load-bearing precedence order: `isStopping` → 'stopping'; else `isGenerating || isExternallyGenerating` → 'generating'; else `hasAudio` → 'ready'; else 'idle'. Poll effect: when `poll && isGenerating`, `setInterval(onRefresh, 1500)` bounded by a `setTimeout` at `GENERATION_POLL_CEILING_MS` that clears the interval and sets a "taking too long" error (port CharacterPreviewRow's WR-02 ceiling recovery, the more complete sibling). Stale-settle effect: watch `isExternallyGenerating` — set the ref true while generating, and on transition-to-false with ref set, clear isGenerating/isStopping. handleGenerate/handleStop wrap the async callbacks with try/catch using `errorMessage(err, ...)`; handleStop uses a finally that clears both flags and calls onRefresh. Export `GspStatus` type and a `UseGenerateStopPlayOptions` interface. The batch site will call this with `poll: false` (it has SSE) or derive inline — support both by honoring the poll flag. No fenced implementation copied blindly: adapt identifiers to the codebase's existing import names.</action>
  <verify>
    <automated>cd frontend && grep -qE 'export (function|const) useGenerateStopPlay' src/hooks/useGenerateStopPlay.ts && grep -q 'GENERATION_POLL_CEILING_MS' src/hooks/useGenerateStopPlay.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - useGenerateStopPlay is exported and imports GENERATION_POLL_CEILING_MS + errorMessage from @/api/client
    - status derivation follows precedence stopping > generating > ready > idle (verifiable by source read of the ternary/if-chain order)
    - The poll effect is gated on both `poll` and `isGenerating` (no interval when poll is false)
    - The hook does NOT import or call useGenerationLock (`grep -c useGenerationLock` returns 0)
    - `npm run typecheck` and `npm run lint` pass
  </acceptance_criteria>
  <done>Hook compiles, lints clean, derives status in the correct precedence order, and never spawns a poll when poll is false.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create GenerateStopPlayButton presentational component</name>
  <files>frontend/src/components/GenerateStopPlayButton.tsx</files>
  <read_first>
    - .planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-UI-SPEC.md — Component Contracts §1 (lines 134-170): the exact prop contract, STATE_CLASSES, STATE_LABEL, and icon/label dispatch rules to copy verbatim
    - frontend/src/components/ui/button.tsx — the Button primitive, its size prop and className merge behavior via cn()
    - frontend/src/components/SegmentTable.tsx — GeneratePlayButton's existing Button usage (lines ~220-239) for the Loader2/Play/Pause icon pattern
    - frontend/src/lib/utils.ts — cn() / tailwind-merge, so state bg-* overrides Button's default variant
  </read_first>
  <action>Create a pure presentational component with props exactly per UI-SPEC §1: `status: GspStatus`, `isPlaying?`, `disabled?`, `disabledReason?`, `size?: "sm" | "default"`, `className?`, `subjectLabel: string`, `onGenerate`, `onStop`, `onTogglePlay`. Define `STATE_CLASSES` (idle `bg-amber-400 text-amber-950 hover:bg-amber-500`, generating/stopping `bg-red-600 text-white hover:bg-red-700`, ready `bg-green-600 text-white hover:bg-green-700`) and `STATE_LABEL` (idle 'Generate Preview', generating 'Stop Generation', stopping 'Stopping…', ready 'Play') exactly as UI-SPEC lists. Render ONE `<Button>` (never a second adjacent button) whose `onClick` dispatches: idle → onGenerate, generating → onStop, ready → onTogglePlay (stopping is disabled, no dispatch). Icon dispatch: idle → no icon (plain text); generating/stopping → `<Loader2 className="animate-spin" />`; ready → `<Play />` when not playing, `<Pause />` when isPlaying. When status is ready and isPlaying, the label flips to 'Pause'. Pass `className={cn(STATE_CLASSES[status], className)}` so the caller's className (e.g. `w-full`) and the state color both apply through Button's default variant. Force `disabled` true whenever `status === "stopping"` OR the caller's disabled prop. Set the `title` attribute to disabledReason when provided. Build the aria-label as `{action} {subjectLabel}` reusing each site's existing wording via subjectLabel. Import GspStatus from the hook file. Do NOT add a new cva variant to button.tsx. Do NOT render error text inside this component (error lives per-call-site).</action>
  <verify>
    <automated>cd frontend && grep -q 'bg-amber-400' src/components/GenerateStopPlayButton.tsx && grep -q 'Generate Preview' src/components/GenerateStopPlayButton.tsx && grep -q 'Stop Generation' src/components/GenerateStopPlayButton.tsx && test $(grep -c '<Button' src/components/GenerateStopPlayButton.tsx) -eq 1 && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - STATE_CLASSES and STATE_LABEL match UI-SPEC §1 exactly (amber/red/green backgrounds; the 4 labels 'Generate Preview' / 'Stop Generation' / 'Stopping…' / 'Play')
    - Exactly one `<Button` element renders (`grep -c '<Button' returns 1`) — no adjacent second button
    - stopping state forces disabled (source: disabled derived includes `status === "stopping"`)
    - No new variant added to button.tsx (`git diff --stat` shows button.tsx untouched)
    - `npm run typecheck` and `npm run lint` pass
  </acceptance_criteria>
  <done>The single-button component renders all 4 states with correct color/label/icon and merges className correctly; typecheck and lint pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend HTTP | Button clicks dispatch to already-existing, already-validated generate/cancel/download endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | outputUrl / downloadUrl string builder | low | accept | Pure template over a server-issued project.id, not user free-text — no injection surface beyond what downloadUrl already accepts (single-user Tailscale boundary per CLAUDE.md) |
| T-07-02 | Information Disclosure | joined-output audio route | low | accept | Reuses the existing /projects/{id}/download route already vetted in Phase 6; no new data exposed; access boundary is Tailscale |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | This phase adds ZERO new packages (RESEARCH Package Legitimacy Audit: not applicable) — no install task, no supply-chain surface |
</threat_model>

<verification>
- `cd frontend && npm run typecheck` passes
- `cd frontend && npm run lint` passes
- Foundation artifacts exist: GenerateStopPlayButton.tsx, useGenerateStopPlay.ts, outputUrl in client.ts
- No new npm dependency added (`git diff frontend/package.json` empty)
</verification>

<success_criteria>
The shared hook and presentational button exist, compile, lint clean, and export the exact prop/label/color contract from UI-SPEC §1; outputUrl reuses the Phase 6 download route. Wave 2 plans can now swap them in.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- `GspStatus` type — `"idle" | "generating" | "stopping" | "ready"` (exported from GenerateStopPlayButton.tsx or the hook)
- `GenerateStopPlayButton` — default/named export, props per UI-SPEC §1 (`status`, `isPlaying`, `disabled`, `disabledReason`, `size`, `className`, `subjectLabel`, `onGenerate`, `onStop`, `onTogglePlay`)
- `useGenerateStopPlay` — hook; options `UseGenerateStopPlayOptions` `{ hasAudio, isExternallyGenerating, poll?, onGenerate, onStop, onRefresh }`; returns `{ status, error, handleGenerate, handleStop }`
- `outputUrl(projectId: string): string` — new export in `frontend/src/api/client.ts`
</artifacts_produced>

<output>
Create `.planning/phases/07-unified-generate-stop-play-button-trimmed-segment-table/07-01-SUMMARY.md` when done
</output>
